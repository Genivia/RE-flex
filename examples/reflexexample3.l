/* Scanner for "C" assignment statements... sort of. */
/* Compile: reflex --bison reflexexample3.l */

%{
#include "y.tab.h"  /* Generated by bison. */
%}

%option bison

%%

[[:digit:]]+  { yylval.num = atoi(text());   return NUMBER; }
[[:alnum:]]+  { yylval.str = strdup(text()); return STRING; }
"="|";"       { return text()[0]; }
.|\n          {}

%%
